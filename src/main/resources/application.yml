spring:
  datasource:
    url : jdbc:mysql://localhost:3306/fabricVendor?useSSL=False
    username : root
    password : Dinesh@12345

#JPA settings
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true